node_modules/
.DS_Store
*.pcapng
*.mp3
*.tgz
package/
.claude/
.serena/

# Vendored native modules — build outputs only; sources are tracked.
vendor/abletonlink-mini/build/
vendor/abletonlink-mini/node_modules/

# Secrets / credentials
.env
.env.*
*.pem
*.key
*.secret
credentials.json
secrets.json
token.json
*.p12
*.pfx

# macOS App bundles
*.app/

# electron-builder output
dist/
release/
out/
*.dmg
*.blockmap
*.exe
*.nsis.7z

# Playwright MCP cache & ad-hoc screenshots
.playwright-mcp/
atelier-*.png

# 코드 그래프 — 개발 도구 산출물, GitHub 공개 불필요.
graphify-out/
.graphify_python

# 내부 문서 / 모듈화 계획 / 디자인 mockup — 공개 안 함.
docs/

# 외부 spec PDF / 참고자료
*.pdf

# 내부 빌드/캡처 분석 산출물
.codex-artifacts/

# 개발용 셋업 가이드 (사용자는 README 만)
SETUP-GUIDE.md
AGENTS.md
CLAUDE.md

# tools/ — 개발 전용. 빌드/배포에 직접 필요 없음 (rename-stub 도 dist:win 이 단순화됨).
tools/

# Development screenshots / scratch images (root + docs)
/grid-*.png
/observatory-*.png
/phrase-*.png
/wfgrid-*.png
*.preview.png
